/**
 * Modern item data components, the replacement for legacy item NBT metadata.
 * Component records are immutable value objects; malformed JSON or invalid
 * constructor arguments throw {@link IllegalArgumentException}.
 */
@NullMarked
package io.fand.api.item.component;

import org.jspecify.annotations.NullMarked;
