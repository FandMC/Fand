# Patch location moved

Canonical paperweight patches now live under `fand-server/patches/`.

Use:

- `./gradlew :fand-server:applyPatches`
- `./gradlew :fand-server:rebuildPatches`

Do not add or edit patch files in this root-level directory.
