From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: MC20018 <winme2001@outlook.com>
Date: Wed, 10 Jun 2026 00:04:17 +0800
Subject: [PATCH] Optimize entity collision candidates


diff --git a/net/minecraft/world/entity/Entity.java b/net/minecraft/world/entity/Entity.java
index 2ae1ca112f8da797623aade20952e63744389a7e..63526d42c7f20d6c30b53e4bb5d7b49c04a015b1 100644
--- a/net/minecraft/world/entity/Entity.java
+++ b/net/minecraft/world/entity/Entity.java
@@ -1097,9 +1097,13 @@ public abstract class Entity
     }
 
     private Vec3 collide(final Vec3 movement) {
+        if (movement.lengthSqr() == 0.0) {
+            return movement;
+        }
+
         AABB aabb = this.getBoundingBox();
         List<VoxelShape> entityColliders = this.level().getEntityCollisions(this, aabb.expandTowards(movement));
-        Vec3 movementStep = movement.lengthSqr() == 0.0 ? movement : collideBoundingBox(this, movement, aabb, this.level(), entityColliders);
+        Vec3 movementStep = collideBoundingBox(this, movement, aabb, this.level(), entityColliders);
         boolean xCollision = movement.x != movementStep.x;
         boolean yCollision = movement.y != movementStep.y;
         boolean zCollision = movement.z != movementStep.z;
diff --git a/net/minecraft/world/entity/vehicle/boat/AbstractBoat.java b/net/minecraft/world/entity/vehicle/boat/AbstractBoat.java
index 700cc60f7b5ef704e69f2a19b8246432764b3fe6..b50ae364b7a312197ae55d6f483b487ee60b3b18 100644
--- a/net/minecraft/world/entity/vehicle/boat/AbstractBoat.java
+++ b/net/minecraft/world/entity/vehicle/boat/AbstractBoat.java
@@ -21,7 +21,6 @@ import net.minecraft.world.InteractionHand;
 import net.minecraft.world.InteractionResult;
 import net.minecraft.world.entity.Entity;
 import net.minecraft.world.entity.EntityDimensions;
-import net.minecraft.world.entity.EntitySelector;
 import net.minecraft.world.entity.EntityType;
 import net.minecraft.world.entity.InterpolationHandler;
 import net.minecraft.world.entity.Leashable;
@@ -280,7 +279,7 @@ public abstract class AbstractBoat extends VehicleEntity implements Leashable {
             }
         }
 
-        List<Entity> entities = this.level().getEntities(this, this.getBoundingBox().inflate(0.2F, -0.01F, 0.2F), EntitySelector.pushableBy(this));
+        List<Entity> entities = this.level().getPushableEntities(this, this.getBoundingBox().inflate(0.2F, -0.01F, 0.2F));
         if (!entities.isEmpty()) {
             boolean addNewPassengers = !this.level().isClientSide() && !(this.getControllingPassenger() instanceof Player);
 
diff --git a/net/minecraft/world/entity/vehicle/minecart/NewMinecartBehavior.java b/net/minecraft/world/entity/vehicle/minecart/NewMinecartBehavior.java
index f131e4d63ea59bac6953849f85fcc289fc25f91f..9c57604f53e27720bac73aa22e0f9f8b8e80cc20 100644
--- a/net/minecraft/world/entity/vehicle/minecart/NewMinecartBehavior.java
+++ b/net/minecraft/world/entity/vehicle/minecart/NewMinecartBehavior.java
@@ -13,6 +13,7 @@ import net.minecraft.server.level.ServerPlayer;
 import net.minecraft.util.Mth;
 import net.minecraft.world.entity.Entity;
 import net.minecraft.world.entity.EntitySelector;
+import net.minecraft.world.level.entity.EntityTypeTest;
 import net.minecraft.world.entity.MoverType;
 import net.minecraft.world.entity.animal.golem.IronGolem;
 import net.minecraft.world.entity.player.Player;
@@ -536,7 +537,7 @@ public class NewMinecartBehavior extends MinecartBehavior {
     public boolean pushEntities(final AABB hitbox) {
         boolean pushed = false;
         if (this.minecart.isRideable()) {
-            List<Entity> entities = this.level().getEntities(this.minecart, hitbox, EntitySelector.pushableBy(this.minecart));
+            List<Entity> entities = this.level().getPushableEntities(this.minecart, hitbox);
             if (!entities.isEmpty()) {
                 for (Entity entity : entities) {
                     if (entity instanceof Player
@@ -550,8 +551,10 @@ public class NewMinecartBehavior extends MinecartBehavior {
                 }
             }
         } else {
-            for (Entity entityx : this.level().getEntities(this.minecart, hitbox)) {
-                if (!this.minecart.hasPassenger(entityx) && entityx.isPushable() && entityx instanceof AbstractMinecart) {
+            for (AbstractMinecart entityx : this.level().getEntities(
+                EntityTypeTest.forClass(AbstractMinecart.class), hitbox, entity -> entity != this.minecart && EntitySelector.NO_SPECTATORS.test(entity)
+            )) {
+                if (!this.minecart.hasPassenger(entityx) && entityx.isPushable()) {
                     entityx.push(this.minecart);
                     pushed = true;
                 }
diff --git a/net/minecraft/world/entity/vehicle/minecart/OldMinecartBehavior.java b/net/minecraft/world/entity/vehicle/minecart/OldMinecartBehavior.java
index f150b4c73870546cd639199d150416a263c87ae5..f942293b6a9426b3ba5c0236d699426f07bacc8a 100644
--- a/net/minecraft/world/entity/vehicle/minecart/OldMinecartBehavior.java
+++ b/net/minecraft/world/entity/vehicle/minecart/OldMinecartBehavior.java
@@ -15,6 +15,7 @@ import net.minecraft.world.entity.InterpolationHandler;
 import net.minecraft.world.entity.MoverType;
 import net.minecraft.world.entity.animal.golem.IronGolem;
 import net.minecraft.world.entity.player.Player;
+import net.minecraft.world.level.entity.EntityTypeTest;
 import net.minecraft.world.level.block.BaseRailBlock;
 import net.minecraft.world.level.block.Blocks;
 import net.minecraft.world.level.block.PoweredRailBlock;
@@ -382,8 +383,10 @@ public class OldMinecartBehavior extends MinecartBehavior {
                 }
             }
         } else {
-            for (Entity entityx : this.level().getEntities(this.minecart, hitbox)) {
-                if (!this.minecart.hasPassenger(entityx) && entityx.isPushable() && entityx instanceof AbstractMinecart) {
+            for (AbstractMinecart entityx : this.level().getEntities(
+                EntityTypeTest.forClass(AbstractMinecart.class), hitbox, entity -> entity != this.minecart && EntitySelector.NO_SPECTATORS.test(entity)
+            )) {
+                if (!this.minecart.hasPassenger(entityx) && entityx.isPushable()) {
                     entityx.push(this.minecart);
                 }
             }
diff --git a/net/minecraft/world/level/Level.java b/net/minecraft/world/level/Level.java
index 305aa7fc9d2a61c751fc83de7fc32b1d4cde137c..54aaae67c176d5f1ad3e000a4f6bea69f0d6d2d5 100644
--- a/net/minecraft/world/level/Level.java
+++ b/net/minecraft/world/level/Level.java
@@ -1,5 +1,7 @@
 package net.minecraft.world.level;
 
+import com.google.common.collect.ImmutableList;
+import com.google.common.collect.ImmutableList.Builder;
 import com.google.common.collect.Lists;
 import com.mojang.serialization.Codec;
 import java.io.IOException;
@@ -87,8 +89,12 @@ import net.minecraft.world.level.storage.LevelData;
 import net.minecraft.world.level.storage.WritableLevelData;
 import net.minecraft.world.phys.AABB;
 import net.minecraft.world.phys.Vec3;
+import net.minecraft.world.phys.shapes.BooleanOp;
+import net.minecraft.world.phys.shapes.Shapes;
+import net.minecraft.world.phys.shapes.VoxelShape;
 import net.minecraft.world.scores.Scoreboard;
 import org.apache.commons.lang3.mutable.MutableBoolean;
+import org.apache.commons.lang3.mutable.MutableObject;
 import org.jspecify.annotations.Nullable;
 
 public abstract class Level implements LevelAccessor, AutoCloseable {
@@ -860,8 +866,135 @@ public abstract class Level implements LevelAccessor, AutoCloseable {
         return hasEntities.isTrue();
     }
 
+    @Override
+    public boolean isUnobstructed(final @Nullable Entity source, final VoxelShape shape) {
+        if (shape.isEmpty()) {
+            return true;
+        }
+        Profiler.get().incrementCounter("getEntities");
+        AABB bounds = shape.bounds();
+        MutableBoolean obstructed = new MutableBoolean();
+        this.getEntities().get(EntityTypeTest.forClass(Entity.class), bounds, entity -> {
+            if (!entity.isRemoved()
+                && entity != source
+                && EntitySelector.NO_SPECTATORS.test(entity)
+                && entity.blocksBuilding
+                && (source == null || !entity.isPassengerOfSameVehicle(source))
+                && Shapes.joinIsNotEmpty(shape, Shapes.create(entity.getBoundingBox()), BooleanOp.AND)) {
+                obstructed.setTrue();
+                return AbortableIterationConsumer.Continuation.ABORT;
+            }
+            return AbortableIterationConsumer.Continuation.CONTINUE;
+        });
+        if (obstructed.isTrue()) {
+            return false;
+        }
+
+        for (EnderDragonPart dragonPart : this.dragonParts()) {
+            if (dragonPart != source
+                && dragonPart.parentMob != source
+                && !dragonPart.isRemoved()
+                && EntitySelector.NO_SPECTATORS.test(dragonPart)
+                && dragonPart.blocksBuilding
+                && (source == null || !dragonPart.isPassengerOfSameVehicle(source))
+                && Shapes.joinIsNotEmpty(shape, Shapes.create(dragonPart.getBoundingBox()), BooleanOp.AND)) {
+                return false;
+            }
+        }
+        return true;
+    }
+
+    @Override
+    public boolean noEntityCollision(final @Nullable Entity entity, final AABB aabb) {
+        if (aabb.getSize() < 1.0E-7) {
+            return true;
+        }
+
+        Profiler.get().incrementCounter("getEntities");
+        Predicate<Entity> canCollide = entity == null ? EntitySelector.CAN_BE_COLLIDED_WITH : EntitySelector.NO_SPECTATORS.and(entity::canCollideWith);
+        AABB searchArea = aabb.inflate(1.0E-7);
+        MutableBoolean collided = new MutableBoolean();
+        this.getEntities().getEntityCollisionCandidates(searchArea, candidate -> {
+            if (candidate != entity && canCollide.test(candidate)) {
+                collided.setTrue();
+                return AbortableIterationConsumer.Continuation.ABORT;
+            }
+            return AbortableIterationConsumer.Continuation.CONTINUE;
+        });
+        if (collided.isTrue()) {
+            return false;
+        }
+
+        for (EnderDragonPart dragonPart : this.dragonParts()) {
+            if (dragonPart != entity && dragonPart.parentMob != entity && canCollide.test(dragonPart) && searchArea.intersects(dragonPart.getBoundingBox())) {
+                return false;
+            }
+        }
+        return true;
+    }
+
+    @Override
+    public List<VoxelShape> getEntityCollisions(final @Nullable Entity source, final AABB testArea) {
+        if (testArea.getSize() < 1.0E-7) {
+            return List.of();
+        }
+
+        Profiler.get().incrementCounter("getEntities");
+        Predicate<Entity> canCollide = source == null ? EntitySelector.CAN_BE_COLLIDED_WITH : EntitySelector.NO_SPECTATORS.and(source::canCollideWith);
+        MutableObject<Builder<VoxelShape>> shapes = new MutableObject<>();
+        MutableBoolean found = new MutableBoolean();
+        AABB searchArea = testArea.inflate(1.0E-7);
+        this.getEntities().getEntityCollisionCandidates(searchArea, entity -> {
+            if (entity != source && canCollide.test(entity)) {
+                found.setTrue();
+                if (shapes.getValue() == null) {
+                    shapes.setValue(ImmutableList.builder());
+                }
+                shapes.getValue().add(Shapes.create(entity.getBoundingBox()));
+            }
+            return AbortableIterationConsumer.Continuation.CONTINUE;
+        });
+
+        for (EnderDragonPart dragonPart : this.dragonParts()) {
+            if (dragonPart != source && dragonPart.parentMob != source && canCollide.test(dragonPart) && searchArea.intersects(dragonPart.getBoundingBox())) {
+                found.setTrue();
+                if (shapes.getValue() == null) {
+                    shapes.setValue(ImmutableList.builder());
+                }
+                shapes.getValue().add(Shapes.create(dragonPart.getBoundingBox()));
+            }
+        }
+
+        return found.isTrue() ? shapes.getValue().build() : List.of();
+    }
+
     public List<Entity> getPushableEntities(final Entity pusher, final AABB boundingBox) {
-        return this.getEntities(pusher, boundingBox, EntitySelector.pushableBy(pusher));
+        Profiler.get().incrementCounter("getEntities");
+        Predicate<Entity> selector = EntitySelector.pushableBy(pusher);
+        MutableObject<List<Entity>> output = new MutableObject<>();
+        this.getEntities().getEntityCollisionCandidates(boundingBox, entity -> {
+            if (entity != pusher && selector.test(entity)) {
+                if (output.getValue() == null) {
+                    output.setValue(Lists.newArrayList());
+                }
+                output.getValue().add(entity);
+            }
+            return AbortableIterationConsumer.Continuation.CONTINUE;
+        });
+
+        for (EnderDragonPart dragonPart : this.dragonParts()) {
+            if (dragonPart != pusher
+                && dragonPart.parentMob != pusher
+                && selector.test(dragonPart)
+                && boundingBox.intersects(dragonPart.getBoundingBox())) {
+                if (output.getValue() == null) {
+                    output.setValue(Lists.newArrayList());
+                }
+                output.getValue().add(dragonPart);
+            }
+        }
+
+        return output.getValue() == null ? List.of() : output.getValue();
     }
 
     public abstract @Nullable Entity getEntity(int id);
diff --git a/net/minecraft/world/level/entity/EntitySection.java b/net/minecraft/world/level/entity/EntitySection.java
index eddcb38b1dcdca18480649369c41f9e84f57e8ec..1fe2fa4a43721a7e0076c828f0e45eb712aa6a51 100644
--- a/net/minecraft/world/level/entity/EntitySection.java
+++ b/net/minecraft/world/level/entity/EntitySection.java
@@ -6,24 +6,37 @@ import java.util.stream.Stream;
 import net.minecraft.util.AbortableIterationConsumer;
 import net.minecraft.util.ClassInstanceMultiMap;
 import net.minecraft.util.VisibleForDebug;
+import net.minecraft.world.entity.Entity;
 import net.minecraft.world.phys.AABB;
 import org.slf4j.Logger;
 
 public class EntitySection<T extends EntityAccess> {
     private static final Logger LOGGER = LogUtils.getLogger();
+    private static final ClassValue<Boolean> ENTITY_COLLISION_CANDIDATE_TYPES = new ClassValue<>() {
+        @Override
+        protected Boolean computeValue(final Class<?> type) {
+            return declaresEntityCollisionMethod(type, "canBeCollidedWith", Entity.class) || declaresEntityCollisionMethod(type, "isPushable");
+        }
+    };
     private final ClassInstanceMultiMap<T> storage;
+    private final ClassInstanceMultiMap<T> entityCollisionCandidates;
     private Visibility chunkStatus;
 
     public EntitySection(final Class<T> entityClass, final Visibility chunkStatus) {
         this.chunkStatus = chunkStatus;
         this.storage = new ClassInstanceMultiMap<>(entityClass);
+        this.entityCollisionCandidates = new ClassInstanceMultiMap<>(entityClass);
     }
 
     public void add(final T entity) {
         this.storage.add(entity);
+        if (couldCollideWithMovingEntity(entity)) {
+            this.entityCollisionCandidates.add(entity);
+        }
     }
 
     public boolean remove(final T entity) {
+        this.entityCollisionCandidates.remove(entity);
         return this.storage.remove(entity);
     }
 
@@ -37,6 +50,16 @@ public class EntitySection<T extends EntityAccess> {
         return AbortableIterationConsumer.Continuation.CONTINUE;
     }
 
+    public AbortableIterationConsumer.Continuation getEntityCollisionCandidates(final AABB bb, final AbortableIterationConsumer<T> entities) {
+        for (T entity : this.entityCollisionCandidates) {
+            if (entity.getBoundingBox().intersects(bb) && entities.accept(entity).shouldAbort()) {
+                return AbortableIterationConsumer.Continuation.ABORT;
+            }
+        }
+
+        return AbortableIterationConsumer.Continuation.CONTINUE;
+    }
+
     public <U extends T> AbortableIterationConsumer.Continuation getEntities(
         final EntityTypeTest<T, U> type, final AABB bb, final AbortableIterationConsumer<? super U> consumer
     ) {
@@ -77,4 +100,18 @@ public class EntitySection<T extends EntityAccess> {
     public int size() {
         return this.storage.size();
     }
+
+    private static boolean couldCollideWithMovingEntity(final EntityAccess entity) {
+        return entity instanceof Entity minecraftEntity
+            && ENTITY_COLLISION_CANDIDATE_TYPES.get(minecraftEntity.getClass());
+    }
+
+    private static boolean declaresEntityCollisionMethod(final Class<?> type, final String name, final Class<?>... parameterTypes) {
+        try {
+            return type.getMethod(name, parameterTypes).getDeclaringClass() != Entity.class;
+        } catch (NoSuchMethodException ex) {
+            LOGGER.warn("Unable to inspect entity collision method {} on {}", name, type.getName(), ex);
+            return true;
+        }
+    }
 }
diff --git a/net/minecraft/world/level/entity/EntitySectionStorage.java b/net/minecraft/world/level/entity/EntitySectionStorage.java
index 1800f2086925a4d6e235fbde093a0f7b8ad91f4e..4d4b8da878ecc238c9501b8802cf17a729deefec 100644
--- a/net/minecraft/world/level/entity/EntitySectionStorage.java
+++ b/net/minecraft/world/level/entity/EntitySectionStorage.java
@@ -121,6 +121,10 @@ public class EntitySectionStorage<T extends EntityAccess> {
         this.forEachAccessibleNonEmptySection(bb, section -> section.getEntities(bb, output));
     }
 
+    public void getEntityCollisionCandidates(final AABB bb, final AbortableIterationConsumer<T> consumer) {
+        this.forEachAccessibleNonEmptySection(bb, section -> section.getEntityCollisionCandidates(bb, consumer));
+    }
+
     public <U extends T> void getEntities(final EntityTypeTest<T, U> type, final AABB bb, final AbortableIterationConsumer<U> consumer) {
         this.forEachAccessibleNonEmptySection(bb, section -> section.getEntities(type, bb, consumer));
     }
diff --git a/net/minecraft/world/level/entity/LevelEntityGetter.java b/net/minecraft/world/level/entity/LevelEntityGetter.java
index 69980918241180346500ef0dbe6d115c57686e6f..33e6fe14f2e486cb68ac6f69276b8ef0b9bc893c 100644
--- a/net/minecraft/world/level/entity/LevelEntityGetter.java
+++ b/net/minecraft/world/level/entity/LevelEntityGetter.java
@@ -17,5 +17,7 @@ public interface LevelEntityGetter<T extends EntityAccess> {
 
     void get(final AABB bb, final Consumer<T> output);
 
+    void getEntityCollisionCandidates(final AABB bb, final AbortableIterationConsumer<T> consumer);
+
     <U extends T> void get(final EntityTypeTest<T, U> type, final AABB bb, final AbortableIterationConsumer<U> consumer);
 }
diff --git a/net/minecraft/world/level/entity/LevelEntityGetterAdapter.java b/net/minecraft/world/level/entity/LevelEntityGetterAdapter.java
index 6f0c1d8596d923a5dbb5d81e31d3b4823c0f5d20..0c20ca2f7406383960e09675c2fe2845700d1701 100644
--- a/net/minecraft/world/level/entity/LevelEntityGetterAdapter.java
+++ b/net/minecraft/world/level/entity/LevelEntityGetterAdapter.java
@@ -40,6 +40,11 @@ public class LevelEntityGetterAdapter<T extends EntityAccess> implements LevelEn
         this.sectionStorage.getEntities(bb, AbortableIterationConsumer.forConsumer(output));
     }
 
+    @Override
+    public void getEntityCollisionCandidates(final AABB bb, final AbortableIterationConsumer<T> consumer) {
+        this.sectionStorage.getEntityCollisionCandidates(bb, consumer);
+    }
+
     @Override
     public <U extends T> void get(final EntityTypeTest<T, U> type, final AABB bb, final AbortableIterationConsumer<U> consumer) {
         this.sectionStorage.getEntities(type, bb, consumer);
