From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: MC20018 <winme2001@outlook.com>
Date: Mon, 8 Jun 2026 18:14:26 +0800
Subject: [PATCH] Complete fine-grained ecosystem event hooks

Add vanilla hook points for the next set of high-value ecosystem events, including player pre-login, armor stand manipulation, book edits, egg throws, level changes, velocity updates, main-hand changes, statistic increments, recipe discovery, block can-build checks, multi-place operations, block cooking, furnace start smelt, structure growth, portal enter/exit/create details, target living entity changes, block damage sources, and block/entity combust sources.

Keep patched Minecraft code thin by routing event construction, cancellation semantics, mutable payload application, and listener failure handling through Fand bridge helpers. Preserve cancellable structure growth by snapshotting generated blocks and restoring both block states and Fand block components when listeners cancel.

diff --git a/net/minecraft/server/level/ServerEntity.java b/net/minecraft/server/level/ServerEntity.java
index b47567c1a7a303044ef768dd2639e2272f51323d..a3afb5e02199dfe62264d63e26ea62f1131cafae 100644
--- a/net/minecraft/server/level/ServerEntity.java
+++ b/net/minecraft/server/level/ServerEntity.java
@@ -219,7 +219,15 @@ public class ServerEntity {
         this.tickCount++;
         if (this.entity.hurtMarked) {
             this.entity.hurtMarked = false;
-            this.synchronizer.sendToTrackingPlayersAndSelf(new ClientboundSetEntityMotionPacket(this.entity));
+            if (this.entity instanceof ServerPlayer fandPlayer) {
+                Vec3 fandVelocity = io.fand.server.event.PlayerEvents.fireVelocity(fandPlayer, this.entity.getDeltaMovement());
+                if (fandVelocity != null) {
+                    this.entity.setDeltaMovement(fandVelocity);
+                    this.synchronizer.sendToTrackingPlayersAndSelf(new ClientboundSetEntityMotionPacket(this.entity.getId(), fandVelocity));
+                }
+            } else {
+                this.synchronizer.sendToTrackingPlayersAndSelf(new ClientboundSetEntityMotionPacket(this.entity));
+            }
         }
     }
 
diff --git a/net/minecraft/server/level/ServerPlayer.java b/net/minecraft/server/level/ServerPlayer.java
index 8889b6b86fdfccd42752de40f67f20e6a9477a6b..57b1d5b799d46e8607b8837349911a244bb7a25d 100644
--- a/net/minecraft/server/level/ServerPlayer.java
+++ b/net/minecraft/server/level/ServerPlayer.java
@@ -540,16 +540,20 @@ public class ServerPlayer extends Player {
 
     public void setExperienceLevels(final int amount) {
         if (amount != this.experienceLevel) {
+            int oldLevel = this.experienceLevel;
             this.experienceLevel = amount;
             this.lastSentExp = -1;
+            io.fand.server.event.PlayerEvents.fireLevelChange(this, oldLevel, this.experienceLevel);
         }
     }
 
     @Override
     public void giveExperienceLevels(final int amount) {
         if (amount != 0) {
+            int oldLevel = this.experienceLevel;
             super.giveExperienceLevels(amount);
             this.lastSentExp = -1;
+            io.fand.server.event.PlayerEvents.fireLevelChange(this, oldLevel, this.experienceLevel);
         }
     }
 
@@ -1580,6 +1584,9 @@ public class ServerPlayer extends Player {
 
     @Override
     public int awardRecipes(final Collection<RecipeHolder<?>> recipes) {
+        if (!io.fand.server.event.PlayerEvents.fireRecipeDiscover(this, recipes)) {
+            return 0;
+        }
         return this.recipeBook.addRecipes(recipes, this);
     }
 
@@ -1616,8 +1623,10 @@ public class ServerPlayer extends Player {
     public void giveExperiencePoints(final int i) {
         int fandAmount = io.fand.server.event.PlayerEvents.fireExperienceChange(this, i);
         if (fandAmount != 0) {
+            int oldLevel = this.experienceLevel;
             super.giveExperiencePoints(fandAmount);
             this.lastSentExp = -1;
+            io.fand.server.event.PlayerEvents.fireLevelChange(this, oldLevel, this.experienceLevel);
         }
     }
 
diff --git a/net/minecraft/server/network/ServerGamePacketListenerImpl.java b/net/minecraft/server/network/ServerGamePacketListenerImpl.java
index 9f27b1b5154857ad8285802611aa355d6c44c131..96ef5ccb191923e5a9165c667a31e9071353bb58 100644
--- a/net/minecraft/server/network/ServerGamePacketListenerImpl.java
+++ b/net/minecraft/server/network/ServerGamePacketListenerImpl.java
@@ -1011,7 +1011,7 @@ public class ServerGamePacketListenerImpl
             title.ifPresent(contents::add);
             contents.addAll(packet.pages());
             Consumer<List<FilteredText>> handler = title.isPresent()
-                ? filteredContents -> this.signBook(filteredContents.get(0), filteredContents.subList(1, filteredContents.size()), slot)
+                ? filteredContents -> this.signBook(packet.title(), filteredContents.get(0), filteredContents.subList(1, filteredContents.size()), slot)
                 : filteredContents -> this.updateBookContents(filteredContents, slot);
             this.filterTextPacket(contents).thenAcceptAsync(handler, this.server);
         }
@@ -1020,21 +1020,38 @@ public class ServerGamePacketListenerImpl
     private void updateBookContents(final List<FilteredText> contents, final int slot) {
         ItemStack carried = this.player.getInventory().getItem(slot);
         if (carried.has(DataComponents.WRITABLE_BOOK_CONTENT)) {
+            ItemStack previousBook = carried.copy();
             List<Filterable<String>> pages = contents.stream().map(this::filterableFromOutgoing).toList();
-            carried.set(DataComponents.WRITABLE_BOOK_CONTENT, new WritableBookContent(pages));
+            ItemStack updatedBook = carried.copy();
+            updatedBook.set(DataComponents.WRITABLE_BOOK_CONTENT, new WritableBookContent(pages));
+            ItemStack fandBook = io.fand.server.event.PlayerEvents.fireEditBook(
+                this.player,
+                slot,
+                previousBook,
+                updatedBook,
+                Optional.empty(),
+                false
+            );
+            if (fandBook != null) {
+                this.player.getInventory().setItem(slot, fandBook);
+            }
         }
     }
 
-    private void signBook(final FilteredText title, final List<FilteredText> contents, final int slot) {
+    private void signBook(final Optional<String> rawTitle, final FilteredText title, final List<FilteredText> contents, final int slot) {
         ItemStack carried = this.player.getInventory().getItem(slot);
         if (carried.has(DataComponents.WRITABLE_BOOK_CONTENT)) {
+            ItemStack previousBook = carried.copy();
             ItemStack writtenBook = carried.transmuteCopy(Items.WRITTEN_BOOK);
             writtenBook.remove(DataComponents.WRITABLE_BOOK_CONTENT);
             List<Filterable<Component>> pages = contents.stream().map(page -> this.filterableFromOutgoing(page).<Component>map(Component::literal)).toList();
             writtenBook.set(
                 DataComponents.WRITTEN_BOOK_CONTENT, new WrittenBookContent(this.filterableFromOutgoing(title), this.player.getPlainTextName(), 0, pages, true)
             );
-            this.player.getInventory().setItem(slot, writtenBook);
+            ItemStack fandBook = io.fand.server.event.PlayerEvents.fireEditBook(this.player, slot, previousBook, writtenBook, rawTitle, true);
+            if (fandBook != null) {
+                this.player.getInventory().setItem(slot, fandBook);
+            }
         }
     }
 
@@ -2269,6 +2286,7 @@ public class ServerGamePacketListenerImpl
         var oldInformation = this.player.clientInformation();
         this.player.updateOptions(packet.information());
         io.fand.server.event.PlayerEvents.fireLocaleChange(this.player, oldInformation.language(), packet.information().language());
+        io.fand.server.event.PlayerEvents.fireChangedMainHand(this.player, oldInformation.mainHand(), packet.information().mainHand());
         if (this.player.isModelPartShown(PlayerModelPart.HAT) != wasHatShown) {
             this.server.getPlayerList().broadcastAll(new ClientboundPlayerInfoUpdatePacket(ClientboundPlayerInfoUpdatePacket.Action.UPDATE_HAT, this.player));
         }
diff --git a/net/minecraft/server/network/ServerLoginPacketListenerImpl.java b/net/minecraft/server/network/ServerLoginPacketListenerImpl.java
index c2aaa52e90d431de03445c937577d77966699dc0..1b0c65c8ae0d1bd68fc7f74449a323de1baaf5af 100644
--- a/net/minecraft/server/network/ServerLoginPacketListenerImpl.java
+++ b/net/minecraft/server/network/ServerLoginPacketListenerImpl.java
@@ -196,6 +196,15 @@ public class ServerLoginPacketListenerImpl implements ServerLoginPacketListener,
     }
 
     private void startClientVerification(final GameProfile profile) {
+        Component fandAsyncPreLoginReason = io.fand.server.event.ServerEvents.fireAsyncPreLogin(
+            this.server,
+            this.connection.getRemoteAddress(),
+            new NameAndId(profile)
+        );
+        if (fandAsyncPreLoginReason != null) {
+            this.disconnect(fandAsyncPreLoginReason);
+            return;
+        }
         this.authenticatedProfile = profile;
         this.state = ServerLoginPacketListenerImpl.State.VERIFYING;
     }
diff --git a/net/minecraft/stats/ServerStatsCounter.java b/net/minecraft/stats/ServerStatsCounter.java
index d8ad3c3191a9bb81f4353f554630393c245deb42..e101b973eee68447155ec8769ad1dc04b6cc38df 100644
--- a/net/minecraft/stats/ServerStatsCounter.java
+++ b/net/minecraft/stats/ServerStatsCounter.java
@@ -91,7 +91,16 @@ public class ServerStatsCounter extends StatsCounter {
 
     @Override
     public void setValue(final Player player, final Stat<?> stat, final int count) {
-        super.setValue(player, stat, count);
+        int previousValue = this.getValue(stat);
+        int nextValue = count;
+        if (player instanceof ServerPlayer serverPlayer) {
+            Integer fandValue = io.fand.server.event.PlayerEvents.fireStatisticIncrement(serverPlayer, stat, previousValue, count);
+            if (fandValue == null) {
+                return;
+            }
+            nextValue = fandValue;
+        }
+        super.setValue(player, stat, nextValue);
         this.dirty.add(stat);
     }
 
diff --git a/net/minecraft/world/entity/Entity.java b/net/minecraft/world/entity/Entity.java
index f9ae61d37fdfa8eee84ff84b00fa0915b7d3364e..2ae1ca112f8da797623aade20952e63744389a7e 100644
--- a/net/minecraft/world/entity/Entity.java
+++ b/net/minecraft/world/entity/Entity.java
@@ -601,14 +601,26 @@ public abstract class Entity
 
     public void lavaIgnite() {
         if (!this.fireImmune()) {
-            this.igniteForSeconds(15.0F);
+            int fandTicks = io.fand.server.event.EntityEvents.fireCombust(
+                this,
+                15 * 20,
+                io.fand.api.event.entity.EntityCombustEvent.Cause.LAVA,
+                null,
+                this.blockPosition()
+            );
+            if (fandTicks > 0) {
+                this.setRemainingFireTicks(fandTicks);
+                this.clearFreeze();
+            }
         }
     }
 
     public void lavaHurt() {
         if (!this.fireImmune()) {
             if (this.level() instanceof ServerLevel serverLevel
-                && this.hurtServer(serverLevel, this.damageSources().lava(), 4.0F)
+                && io.fand.server.event.EntityEvents.withBlockDamageSource(
+                    this.blockPosition(),
+                    () -> this.hurtServer(serverLevel, this.damageSources().lava(), 4.0F))
                 && this.shouldPlayLavaHurtSound()
                 && !this.isSilent()) {
                 serverLevel.playSound(
@@ -2578,6 +2590,7 @@ public abstract class Entity
     }
 
     public void setAsInsidePortal(final Portal portal, final BlockPos pos) {
+        io.fand.server.event.EntityEvents.firePortalEnter(this, pos);
         if (this.isOnPortalCooldown()) {
             this.setPortalCooldown();
         } else {
@@ -2606,15 +2619,21 @@ public abstract class Entity
                                 ? io.fand.server.event.PlayerEvents.firePortal(serverPlayer, teleportTransition)
                                 : io.fand.server.event.EntityEvents.firePortal(this, teleportTransition);
                             if (fandTransition != null) {
+                                if (!(this instanceof ServerPlayer)) {
+                                    fandTransition = io.fand.server.event.EntityEvents.firePortalExit(this, fandTransition);
+                                }
+                            }
+                            if (fandTransition != null) {
+                                TeleportTransition finalTransition = fandTransition;
                                 if (this instanceof ServerPlayer serverPlayer) {
                                     io.fand.server.event.PlayerEvents.withTeleportCause(
                                         io.fand.api.event.player.PlayerTeleportEvent.Cause.PORTAL,
-                                        () -> this.teleport(fandTransition)
+                                        () -> this.teleport(finalTransition)
                                     );
                                 } else {
                                     io.fand.server.event.EntityEvents.withTeleportCause(
                                         io.fand.api.event.entity.EntityTeleportEvent.Cause.PORTAL,
-                                        () -> this.teleport(fandTransition)
+                                        () -> this.teleport(finalTransition)
                                     );
                                 }
                             }
diff --git a/net/minecraft/world/entity/decoration/ArmorStand.java b/net/minecraft/world/entity/decoration/ArmorStand.java
index 20614240580e50d750998c99d3b372d0a38adeaa..aef3fe608cd920db6e05b96480482b6dac005d8b 100644
--- a/net/minecraft/world/entity/decoration/ArmorStand.java
+++ b/net/minecraft/world/entity/decoration/ArmorStand.java
@@ -248,6 +248,9 @@ public class ArmorStand extends LivingEntity {
             return false;
         } else if (itemStack.isEmpty() && (this.disabledSlots & 1 << slot.getFilterBit(16)) != 0) {
             return false;
+        } else if (player instanceof net.minecraft.server.level.ServerPlayer serverPlayer
+            && !io.fand.server.event.PlayerEvents.fireArmorStandManipulate(serverPlayer, this, slot, playerItemStack, itemStack)) {
+            return false;
         } else if (player.hasInfiniteMaterials() && itemStack.isEmpty() && !playerItemStack.isEmpty()) {
             this.setItemSlot(slot, playerItemStack.copyWithCount(1));
             return true;
diff --git a/net/minecraft/world/entity/player/Player.java b/net/minecraft/world/entity/player/Player.java
index 770bea0d44ee942be29c525cf4082b739ad754d5..dfa9436d5ccef103eeea939ef1fcf16590167120 100644
--- a/net/minecraft/world/entity/player/Player.java
+++ b/net/minecraft/world/entity/player/Player.java
@@ -1119,7 +1119,12 @@ public abstract class Player extends Avatar implements ContainerUser {
         }
 
         if (entity instanceof ServerPlayer && entity.hurtMarked) {
-            ((ServerPlayer)entity).connection.send(new ClientboundSetEntityMotionPacket(entity));
+            ServerPlayer fandTarget = (ServerPlayer)entity;
+            Vec3 fandVelocity = io.fand.server.event.PlayerEvents.fireVelocity(fandTarget, entity.getDeltaMovement());
+            if (fandVelocity != null) {
+                entity.setDeltaMovement(fandVelocity);
+                fandTarget.connection.send(new ClientboundSetEntityMotionPacket(entity.getId(), fandVelocity));
+            }
             entity.hurtMarked = false;
             entity.setDeltaMovement(oldMovement);
         }
diff --git a/net/minecraft/world/entity/projectile/hurtingprojectile/SmallFireball.java b/net/minecraft/world/entity/projectile/hurtingprojectile/SmallFireball.java
index be73a5d5b155c5306e2c481e6a0412438d935886..367b1fea46039eadef82fd2f467ff9e7cbd50b87 100644
--- a/net/minecraft/world/entity/projectile/hurtingprojectile/SmallFireball.java
+++ b/net/minecraft/world/entity/projectile/hurtingprojectile/SmallFireball.java
@@ -36,7 +36,17 @@ public class SmallFireball extends Fireball {
             Entity var7 = hitResult.getEntity();
             Entity owner = this.getOwner();
             int remainingFireTicks = var7.getRemainingFireTicks();
-            var7.igniteForSeconds(5.0F);
+            int fandFireTicks = io.fand.server.event.EntityEvents.fireCombust(
+                var7,
+                5 * 20,
+                io.fand.api.event.entity.EntityCombustEvent.Cause.PROJECTILE,
+                owner == null ? this : owner,
+                null
+            );
+            if (fandFireTicks > 0) {
+                var7.setRemainingFireTicks(fandFireTicks);
+                var7.clearFreeze();
+            }
             DamageSource damageSource = this.damageSources().fireball(this, owner);
             if (!var7.hurtServer(serverLevel, damageSource, 5.0F)) {
                 var7.setRemainingFireTicks(remainingFireTicks);
diff --git a/net/minecraft/world/entity/projectile/throwableitemprojectile/ThrownEgg.java b/net/minecraft/world/entity/projectile/throwableitemprojectile/ThrownEgg.java
index dc6e875951c3ccbfec086e4d11dd7d3b9dc40612..1ce1844c931e7969bd764dd5cc8828dac5fcd12b 100644
--- a/net/minecraft/world/entity/projectile/throwableitemprojectile/ThrownEgg.java
+++ b/net/minecraft/world/entity/projectile/throwableitemprojectile/ThrownEgg.java
@@ -66,12 +66,15 @@ public class ThrownEgg extends ThrowableItemProjectile {
     protected void onHit(final HitResult hitResult) {
         super.onHit(hitResult);
         if (!this.level().isClientSide()) {
-            if (this.random.nextInt(8) == 0) {
-                int count = 1;
-                if (this.random.nextInt(32) == 0) {
-                    count = 4;
-                }
+            boolean hatching = this.random.nextInt(8) == 0;
+            int count = hatching && this.random.nextInt(32) == 0 ? 4 : 1;
+            if (this.getOwner() instanceof net.minecraft.server.level.ServerPlayer fandPlayer) {
+                var fandEgg = io.fand.server.event.PlayerEvents.fireEggThrow(fandPlayer, this, hatching, hatching ? count : 0);
+                hatching = fandEgg.hatching();
+                count = fandEgg.hatchCount();
+            }
 
+            if (hatching) {
                 for (int i = 0; i < count; i++) {
                     Chicken chicken = EntityType.CHICKEN.create(this.level(), EntitySpawnReason.TRIGGERED);
                     if (chicken != null) {
diff --git a/net/minecraft/world/item/BlockItem.java b/net/minecraft/world/item/BlockItem.java
index 31e62e9f986f8b9b2d87ebe94978c7a9e3be8d89..bb4921c7621a5eac4e9543ef3cdfc5610c2f8955 100644
--- a/net/minecraft/world/item/BlockItem.java
+++ b/net/minecraft/world/item/BlockItem.java
@@ -1,6 +1,9 @@
 package net.minecraft.world.item;
 
 import java.util.Map;
+import java.util.ArrayList;
+import java.util.HashMap;
+import java.util.List;
 import net.minecraft.advancements.CriteriaTriggers;
 import net.minecraft.core.BlockPos;
 import net.minecraft.core.component.DataComponents;
@@ -66,34 +69,17 @@ public class BlockItem extends Item {
                     io.fand.api.component.DataComponentMap fandReplacedComponents = fandLevel instanceof net.minecraft.server.level.ServerLevel fandServerLevel
                             ? io.fand.server.component.BlockComponentStorage.snapshot(fandServerLevel, fandPos)
                             : io.fand.api.component.DataComponentMap.EMPTY;
+                    boolean fandTrackPlacement = fandLevel instanceof net.minecraft.server.level.ServerLevel
+                            && fandContextPlayer instanceof ServerPlayer
+                            && io.fand.server.hooks.FandHooks.hasListeners(io.fand.api.event.block.BlockPlaceEvent.class);
+                    Map<BlockPos, BlockState> fandBeforeStates = fandTrackPlacement ? snapshotPlacementStates(fandLevel, fandPos) : Map.of();
+                    Map<BlockPos, io.fand.api.component.DataComponentMap> fandBeforeComponents = fandTrackPlacement
+                            && fandLevel instanceof net.minecraft.server.level.ServerLevel fandServerLevel
+                            ? snapshotPlacementComponents(fandServerLevel, fandBeforeStates.keySet())
+                            : Map.of();
                     if (!this.placeBlock(updatedPlaceContext, placementState)) {
                         return InteractionResult.FAIL;
                     }
-                    if (fandLevel instanceof net.minecraft.server.level.ServerLevel fandServerLevel
-                            && fandContextPlayer instanceof ServerPlayer fandServerPlayer
-                            && io.fand.server.hooks.FandHooks.hasListeners(io.fand.api.event.block.BlockPlaceEvent.class)) {
-                        var fandPlayer = io.fand.server.hooks.FandHooks.findPlayer(fandServerPlayer.getUUID());
-                        if (fandPlayer != null) {
-                            var fandWorld = io.fand.server.hooks.FandHooks.wrapWorld(fandServerLevel);
-                            if (fandWorld != null) {
-                                BlockState fandPlacedState = fandLevel.getBlockState(fandPos);
-                                var fandBlock = new io.fand.server.block.FandBlock(fandWorld, fandPos.getX(), fandPos.getY(), fandPos.getZ());
-                                var fandPlacedType = io.fand.server.block.FandBlockType.of(fandPlacedState.getBlock());
-                                var fandReplacedType = io.fand.server.block.FandBlockType.of(fandReplacedState.getBlock());
-                                var fandPlaceEvent = io.fand.server.hooks.FandHooks.fireOrLog(
-                                        new io.fand.api.event.block.BlockPlaceEvent(fandPlayer, fandBlock, fandPlacedType, fandReplacedType),
-                                        "BlockPlaceEvent");
-                                if (fandPlaceEvent.cancelled()) {
-                                    fandLevel.setBlock(fandPos, fandReplacedState, Block.UPDATE_ALL);
-                                    if (!fandReplacedComponents.isEmpty()) {
-                                        io.fand.server.component.BlockComponentStorage.put(fandServerLevel, fandPos, fandReplacedComponents);
-                                    }
-                                    fandServerPlayer.connection.send(new net.minecraft.network.protocol.game.ClientboundBlockUpdatePacket(fandLevel, fandPos));
-                                    return InteractionResult.FAIL;
-                                }
-                            }
-                        }
-                    }
                     BlockPos pos = fandPos;
                     Level level = fandLevel;
                     Player player = fandContextPlayer;
@@ -105,6 +91,31 @@ public class BlockItem extends Item {
                         updateBlockEntityComponents(level, pos, itemStack);
                         placedState.getBlock().setPlacedBy(level, pos, placedState, player, itemStack);
                         if (player instanceof ServerPlayer) {
+                            if (fandTrackPlacement && level instanceof net.minecraft.server.level.ServerLevel fandServerLevel) {
+                                ServerPlayer fandServerPlayer = (ServerPlayer)player;
+                                List<BlockPos> fandChangedPositions = changedPlacementPositions(level, fandBeforeStates);
+                                boolean fandAllowed = fandChangedPositions.size() > 1
+                                        ? io.fand.server.event.BlockEvents.fireMultiPlace(
+                                                fandServerLevel,
+                                                pos,
+                                                fandServerPlayer,
+                                                placedState,
+                                                fandReplacedState,
+                                                fandChangedPositions)
+                                        : io.fand.server.event.BlockEvents.firePlace(
+                                                fandServerLevel,
+                                                pos,
+                                                fandServerPlayer,
+                                                placedState,
+                                                fandReplacedState);
+                                if (!fandAllowed) {
+                                    restorePlacement(fandServerLevel, fandBeforeStates, fandBeforeComponents, fandServerPlayer);
+                                    if (!fandReplacedComponents.isEmpty()) {
+                                        io.fand.server.component.BlockComponentStorage.put(fandServerLevel, fandPos, fandReplacedComponents);
+                                    }
+                                    return InteractionResult.FAIL;
+                                }
+                            }
                             CriteriaTriggers.PLACED_BLOCK.trigger((ServerPlayer)player, pos, itemStack);
                         }
                     }
@@ -145,7 +156,21 @@ public class BlockItem extends Item {
 
     protected @Nullable BlockState getPlacementState(final BlockPlaceContext context) {
         BlockState stateForPlacement = this.getBlock().getStateForPlacement(context);
-        return stateForPlacement != null && this.canPlace(context, stateForPlacement) ? stateForPlacement : null;
+        if (stateForPlacement == null) {
+            return null;
+        }
+        boolean canPlace = this.canPlace(context, stateForPlacement);
+        Level level = context.getLevel();
+        if (level instanceof net.minecraft.server.level.ServerLevel serverLevel) {
+            canPlace = io.fand.server.event.BlockEvents.fireCanBuild(
+                    serverLevel,
+                    context.getClickedPos(),
+                    context.getPlayer() instanceof ServerPlayer serverPlayer ? serverPlayer : null,
+                    context.getItemInHand(),
+                    stateForPlacement,
+                    canPlace);
+        }
+        return canPlace ? stateForPlacement : null;
     }
 
     private BlockState updateBlockStateFromTag(final BlockPos pos, final Level level, final ItemStack itemStack, final BlockState placedState) {
@@ -217,6 +242,57 @@ public class BlockItem extends Item {
         return this.block;
     }
 
+    private static Map<BlockPos, BlockState> snapshotPlacementStates(final Level level, final BlockPos center) {
+        Map<BlockPos, BlockState> states = new HashMap<>();
+        for (BlockPos pos : BlockPos.betweenClosed(center.offset(-1, -1, -1), center.offset(1, 2, 1))) {
+            states.put(pos.immutable(), level.getBlockState(pos));
+        }
+        return states;
+    }
+
+    private static Map<BlockPos, io.fand.api.component.DataComponentMap> snapshotPlacementComponents(
+            final net.minecraft.server.level.ServerLevel level,
+            final Iterable<BlockPos> positions
+    ) {
+        Map<BlockPos, io.fand.api.component.DataComponentMap> components = new HashMap<>();
+        for (BlockPos pos : positions) {
+            components.put(pos, io.fand.server.component.BlockComponentStorage.snapshot(level, pos));
+        }
+        return components;
+    }
+
+    private static List<BlockPos> changedPlacementPositions(final Level level, final Map<BlockPos, BlockState> beforeStates) {
+        List<BlockPos> changed = new ArrayList<>();
+        for (Map.Entry<BlockPos, BlockState> entry : beforeStates.entrySet()) {
+            if (!level.getBlockState(entry.getKey()).equals(entry.getValue())) {
+                changed.add(entry.getKey());
+            }
+        }
+        if (changed.isEmpty()) {
+            changed.addAll(beforeStates.keySet().stream().filter(pos -> !level.getBlockState(pos).isAir()).limit(1).toList());
+        }
+        return changed;
+    }
+
+    private static void restorePlacement(
+            final net.minecraft.server.level.ServerLevel level,
+            final Map<BlockPos, BlockState> beforeStates,
+            final Map<BlockPos, io.fand.api.component.DataComponentMap> beforeComponents,
+            final ServerPlayer player
+    ) {
+        for (Map.Entry<BlockPos, BlockState> entry : beforeStates.entrySet()) {
+            BlockPos pos = entry.getKey();
+            level.setBlock(pos, entry.getValue(), Block.UPDATE_ALL);
+            io.fand.api.component.DataComponentMap components = beforeComponents.getOrDefault(pos, io.fand.api.component.DataComponentMap.EMPTY);
+            if (components.isEmpty()) {
+                io.fand.server.component.BlockComponentStorage.clear(level, pos);
+            } else {
+                io.fand.server.component.BlockComponentStorage.put(level, pos, components);
+            }
+            player.connection.send(new net.minecraft.network.protocol.game.ClientboundBlockUpdatePacket(level, pos));
+        }
+    }
+
     public void registerBlocks(final Map<Block, Item> map, final Item item) {
         map.put(this.getBlock(), item);
     }
diff --git a/net/minecraft/world/item/BoneMealItem.java b/net/minecraft/world/item/BoneMealItem.java
index a3ee3c7f2ae837226bf290551b9f5078b71667df..deb36af7a8f45b46ffa0ae23599c14e10338a552 100644
--- a/net/minecraft/world/item/BoneMealItem.java
+++ b/net/minecraft/world/item/BoneMealItem.java
@@ -82,7 +82,15 @@ public class BoneMealItem extends Item {
                     return false;
                 }
                 if (block.isBonemealSuccess(level, level.getRandom(), pos, state)) {
-                    block.performBonemeal((ServerLevel)level, level.getRandom(), pos, state);
+                    if (!io.fand.server.event.BlockEvents.withStructureGrowContext(
+                        player instanceof ServerPlayer serverPlayer ? serverPlayer : null,
+                        true,
+                        () -> {
+                            block.performBonemeal((ServerLevel)level, level.getRandom(), pos, state);
+                            return true;
+                        })) {
+                        return false;
+                    }
                 }
 
                 itemStack.shrink(1);
diff --git a/net/minecraft/world/item/MaceItem.java b/net/minecraft/world/item/MaceItem.java
index fcd959584e1b9d991e8ada0fadd86018c1ea50f9..ec6e96cdfc87767a3266d672c2b4d1b397877cd5 100644
--- a/net/minecraft/world/item/MaceItem.java
+++ b/net/minecraft/world/item/MaceItem.java
@@ -59,7 +59,11 @@ public class MaceItem extends Item {
             attacker.setDeltaMovement(attacker.getDeltaMovement().with(Direction.Axis.Y, 0.01F));
             attacker.setIgnoreFallDamageFromCurrentImpulse(true, this.calculateImpactPosition(attacker));
             if (attacker instanceof ServerPlayer player) {
-                player.connection.send(new ClientboundSetEntityMotionPacket(player));
+                Vec3 fandVelocity = io.fand.server.event.PlayerEvents.fireVelocity(player, player.getDeltaMovement());
+                if (fandVelocity != null) {
+                    player.setDeltaMovement(fandVelocity);
+                    player.connection.send(new ClientboundSetEntityMotionPacket(player.getId(), fandVelocity));
+                }
             }
 
             if (mob.onGround()) {
@@ -126,7 +130,11 @@ public class MaceItem extends Item {
             if (knockbackPower > 0.0) {
                 nearby.push(knockbackVector.x, 0.7F, knockbackVector.z);
                 if (nearby instanceof ServerPlayer otherPlayer) {
-                    otherPlayer.connection.send(new ClientboundSetEntityMotionPacket(otherPlayer));
+                    Vec3 fandVelocity = io.fand.server.event.PlayerEvents.fireVelocity(otherPlayer, otherPlayer.getDeltaMovement());
+                    if (fandVelocity != null) {
+                        otherPlayer.setDeltaMovement(fandVelocity);
+                        otherPlayer.connection.send(new ClientboundSetEntityMotionPacket(otherPlayer.getId(), fandVelocity));
+                    }
                 }
             }
         });
diff --git a/net/minecraft/world/level/block/BaseFireBlock.java b/net/minecraft/world/level/block/BaseFireBlock.java
index f657a5436cf1998f75c4800c9742de22459f20d1..2af563ab56faa4285f8b9996c959c0cb9e7d980b 100644
--- a/net/minecraft/world/level/block/BaseFireBlock.java
+++ b/net/minecraft/world/level/block/BaseFireBlock.java
@@ -137,8 +137,14 @@ public abstract class BaseFireBlock extends Block {
         final boolean isPrecise
     ) {
         effectApplier.apply(InsideBlockEffectType.CLEAR_FREEZE);
-        effectApplier.apply(InsideBlockEffectType.FIRE_IGNITE);
-        effectApplier.runAfter(InsideBlockEffectType.FIRE_IGNITE, e -> e.hurt(e.level().damageSources().inFire(), this.fireDamage));
+        effectApplier.runAfter(InsideBlockEffectType.CLEAR_FREEZE, target -> fireIgnite(target, pos));
+        effectApplier.runAfter(InsideBlockEffectType.CLEAR_FREEZE, e -> io.fand.server.event.EntityEvents.withBlockDamageSource(
+            pos,
+            () -> {
+                e.hurt(e.level().damageSources().inFire(), this.fireDamage);
+                return true;
+            }
+        ));
     }
 
     public static void fireIgnite(final Entity entity) {
@@ -156,6 +162,31 @@ public abstract class BaseFireBlock extends Block {
         }
     }
 
+    public static void fireIgnite(final Entity entity, final BlockPos sourcePos) {
+        if (!entity.fireImmune()) {
+            if (entity.getRemainingFireTicks() < 0) {
+                entity.setRemainingFireTicks(entity.getRemainingFireTicks() + 1);
+            } else if (entity instanceof ServerPlayer) {
+                int addedFireTicks = entity.level().getRandom().nextInt(1, 3);
+                entity.setRemainingFireTicks(entity.getRemainingFireTicks() + addedFireTicks);
+            }
+
+            if (entity.getRemainingFireTicks() >= 0) {
+                int ticks = io.fand.server.event.EntityEvents.fireCombust(
+                    entity,
+                    8 * 20,
+                    io.fand.api.event.entity.EntityCombustEvent.Cause.FIRE,
+                    null,
+                    sourcePos
+                );
+                if (ticks > 0) {
+                    entity.setRemainingFireTicks(ticks);
+                    entity.clearFreeze();
+                }
+            }
+        }
+    }
+
     @Override
     protected void onPlace(final BlockState state, final Level level, final BlockPos pos, final BlockState oldState, final boolean movedByPiston) {
         if (!oldState.is(state.getBlock())) {
diff --git a/net/minecraft/world/level/block/CactusBlock.java b/net/minecraft/world/level/block/CactusBlock.java
index 7d908424cf41684def5bc2aa014a4ee92f47d0f2..1f8160cd2e8f1b4ece3aced0129e295a3b544dfb 100644
--- a/net/minecraft/world/level/block/CactusBlock.java
+++ b/net/minecraft/world/level/block/CactusBlock.java
@@ -131,7 +131,13 @@ public class CactusBlock extends Block {
         final InsideBlockEffectApplier effectApplier,
         final boolean isPrecise
     ) {
-        entity.hurt(level.damageSources().cactus(), 1.0F);
+        io.fand.server.event.EntityEvents.withBlockDamageSource(
+            pos,
+            () -> {
+                entity.hurt(level.damageSources().cactus(), 1.0F);
+                return true;
+            }
+        );
     }
 
     @Override
diff --git a/net/minecraft/world/level/block/MagmaBlock.java b/net/minecraft/world/level/block/MagmaBlock.java
index 6700437e22a7dd0f29af24bf7740ea4399dc4008..09e48c6b18cc5020cde004337c80dbd4bc81f9ba 100644
--- a/net/minecraft/world/level/block/MagmaBlock.java
+++ b/net/minecraft/world/level/block/MagmaBlock.java
@@ -23,7 +23,13 @@ public class MagmaBlock extends Block {
     @Override
     public void stepOn(final Level level, final BlockPos pos, final BlockState onState, final Entity entity) {
         if (!entity.isSteppingCarefully() && entity instanceof LivingEntity) {
-            entity.hurt(level.damageSources().hotFloor(), 1.0F);
+            io.fand.server.event.EntityEvents.withBlockDamageSource(
+                pos,
+                () -> {
+                    entity.hurt(level.damageSources().hotFloor(), 1.0F);
+                    return true;
+                }
+            );
         }
 
         super.stepOn(level, pos, onState, entity);
diff --git a/net/minecraft/world/level/block/MushroomBlock.java b/net/minecraft/world/level/block/MushroomBlock.java
index 5d482a8df571566f8f46731db81185dba51af608..197afd2492cc0c6e27aba3cd7d33097ea2c93fa4 100644
--- a/net/minecraft/world/level/block/MushroomBlock.java
+++ b/net/minecraft/world/level/block/MushroomBlock.java
@@ -91,11 +91,17 @@ public class MushroomBlock extends VegetationBlock implements BonemealableBlock
         if (feature.isEmpty()) {
             return false;
         } else {
+            io.fand.server.event.BlockEvents.StructureGrowSnapshot fandSnapshot =
+                io.fand.server.event.BlockEvents.captureStructureGrow(level, pos);
             level.removeBlock(pos, false);
             if (feature.get().value().place(level, level.getChunkSource().getGenerator(), random, pos)) {
-                return true;
+                return io.fand.server.event.BlockEvents.finishStructureGrow(level, pos, fandSnapshot);
             } else {
-                level.setBlock(pos, state, Block.UPDATE_ALL);
+                if (fandSnapshot != null) {
+                    io.fand.server.event.BlockEvents.restoreStructureGrow(level, fandSnapshot);
+                } else {
+                    level.setBlock(pos, state, Block.UPDATE_ALL);
+                }
                 return false;
             }
         }
diff --git a/net/minecraft/world/level/block/NetherPortalBlock.java b/net/minecraft/world/level/block/NetherPortalBlock.java
index 6ed3a44d417f4eef3d3041c4ae04a2b0867b8b5f..c6dceb0a716f1e939c8b1eb5592704890370ae50 100644
--- a/net/minecraft/world/level/block/NetherPortalBlock.java
+++ b/net/minecraft/world/level/block/NetherPortalBlock.java
@@ -171,7 +171,7 @@ public class NetherPortalBlock extends Block implements Portal {
             post = TeleportTransition.PLAY_PORTAL_SOUND.then(e -> e.placePortalTicket(pos));
         } else {
             Direction.Axis sourcePortalAxis = entity.level().getBlockState(portalEntryPos).getOptionalValue(AXIS).orElse(Direction.Axis.X);
-            Optional<BlockUtil.FoundRectangle> createdExit = newLevel.getPortalForcer().createPortal(approximateExitPos, sourcePortalAxis);
+            Optional<BlockUtil.FoundRectangle> createdExit = newLevel.getPortalForcer().createPortal(approximateExitPos, sourcePortalAxis, entity);
             if (createdExit.isEmpty()) {
                 LOGGER.error("Unable to create a portal, likely target out of worldborder");
                 return null;
diff --git a/net/minecraft/world/level/block/SweetBerryBushBlock.java b/net/minecraft/world/level/block/SweetBerryBushBlock.java
index cd4da6c75bb749921d8a7c2d35b7d6bd19d268e7..f08afb52bfc627beceee02efdbc768ead684c097 100644
--- a/net/minecraft/world/level/block/SweetBerryBushBlock.java
+++ b/net/minecraft/world/level/block/SweetBerryBushBlock.java
@@ -95,7 +95,13 @@ public class SweetBerryBushBlock extends VegetationBlock implements Bonemealable
                     double xs = Math.abs(movement.x());
                     double zs = Math.abs(movement.z());
                     if (xs >= 0.003F || zs >= 0.003F) {
-                        entity.hurtServer(serverLevel, level.damageSources().sweetBerryBush(), 1.0F);
+                        io.fand.server.event.EntityEvents.withBlockDamageSource(
+                            pos,
+                            () -> {
+                                entity.hurtServer(serverLevel, level.damageSources().sweetBerryBush(), 1.0F);
+                                return true;
+                            }
+                        );
                     }
                 }
             }
diff --git a/net/minecraft/world/level/block/entity/AbstractFurnaceBlockEntity.java b/net/minecraft/world/level/block/entity/AbstractFurnaceBlockEntity.java
index 74516c411ed56f2695e762b04b4451d1feae0a49..2f58e5c7bd524b13b47e28e1a3b97f0251364ab4 100644
--- a/net/minecraft/world/level/block/entity/AbstractFurnaceBlockEntity.java
+++ b/net/minecraft/world/level/block/entity/AbstractFurnaceBlockEntity.java
@@ -166,6 +166,20 @@ public abstract class AbstractFurnaceBlockEntity extends BaseContainerBlockEntit
                     int maxStackSize = entity.getMaxStackSize();
                     ItemStack burnResult = recipe.value().assemble(input);
                     if (!burnResult.isEmpty() && canBurn(entity.items, maxStackSize, burnResult)) {
+                        if (entity.cookingTimer == 0) {
+                            int fandCookingTotalTime = io.fand.server.inventory.InventoryEvents.fireFurnaceStartSmelt(
+                                level,
+                                pos,
+                                entity,
+                                java.util.Optional.of(recipe),
+                                ingredient,
+                                recipe.value().cookingTime()
+                            );
+                            if (fandCookingTotalTime <= 0) {
+                                return;
+                            }
+                            entity.cookingTotalTime = fandCookingTotalTime;
+                        }
                         if (!isLit) {
                             int newLitTime = entity.getBurnDuration(level.fuelValues(), fuel);
                             newLitTime = io.fand.server.inventory.InventoryEvents.fireFurnaceBurn(level, pos, entity, fuel, newLitTime);
diff --git a/net/minecraft/world/level/block/grower/TreeGrower.java b/net/minecraft/world/level/block/grower/TreeGrower.java
index 9c898521851528c0625e019a72b69a3916e5da0a..d4bb67606af60f00a7bf8b9bc45aba8611a6b31f 100644
--- a/net/minecraft/world/level/block/grower/TreeGrower.java
+++ b/net/minecraft/world/level/block/grower/TreeGrower.java
@@ -136,19 +136,26 @@ public final class TreeGrower {
                     for (int dz = 0; dz >= -1; dz--) {
                         if (isTwoByTwoSapling(state, level, pos, dx, dz)) {
                             ConfiguredFeature<?, ?> feature = featureHolder.value();
+                            BlockPos origin = pos.offset(dx, 0, dz);
+                            io.fand.server.event.BlockEvents.StructureGrowSnapshot fandSnapshot =
+                                io.fand.server.event.BlockEvents.captureStructureGrow(level, origin);
                             BlockState air = Blocks.AIR.defaultBlockState();
                             level.setBlock(pos.offset(dx, 0, dz), air, Block.UPDATE_NONE);
                             level.setBlock(pos.offset(dx + 1, 0, dz), air, Block.UPDATE_NONE);
                             level.setBlock(pos.offset(dx, 0, dz + 1), air, Block.UPDATE_NONE);
                             level.setBlock(pos.offset(dx + 1, 0, dz + 1), air, Block.UPDATE_NONE);
-                            if (feature.place(level, generator, random, pos.offset(dx, 0, dz))) {
-                                return true;
+                            if (feature.place(level, generator, random, origin)) {
+                                return io.fand.server.event.BlockEvents.finishStructureGrow(level, origin, fandSnapshot);
                             }
 
-                            level.setBlock(pos.offset(dx, 0, dz), state, Block.UPDATE_NONE);
-                            level.setBlock(pos.offset(dx + 1, 0, dz), state, Block.UPDATE_NONE);
-                            level.setBlock(pos.offset(dx, 0, dz + 1), state, Block.UPDATE_NONE);
-                            level.setBlock(pos.offset(dx + 1, 0, dz + 1), state, Block.UPDATE_NONE);
+                            if (fandSnapshot != null) {
+                                io.fand.server.event.BlockEvents.restoreStructureGrow(level, fandSnapshot);
+                            } else {
+                                level.setBlock(pos.offset(dx, 0, dz), state, Block.UPDATE_NONE);
+                                level.setBlock(pos.offset(dx + 1, 0, dz), state, Block.UPDATE_NONE);
+                                level.setBlock(pos.offset(dx, 0, dz + 1), state, Block.UPDATE_NONE);
+                                level.setBlock(pos.offset(dx + 1, 0, dz + 1), state, Block.UPDATE_NONE);
+                            }
                             return false;
                         }
                     }
@@ -166,15 +173,21 @@ public final class TreeGrower {
             } else {
                 ConfiguredFeature<?, ?> feature = featureHolder.value();
                 BlockState emptyBlock = level.getFluidState(pos).createLegacyBlock();
+                io.fand.server.event.BlockEvents.StructureGrowSnapshot fandSnapshot =
+                    io.fand.server.event.BlockEvents.captureStructureGrow(level, pos);
                 level.setBlock(pos, emptyBlock, Block.UPDATE_NONE);
                 if (feature.place(level, generator, random, pos)) {
                     if (level.getBlockState(pos) == emptyBlock) {
                         level.sendBlockUpdated(pos, state, emptyBlock, Block.UPDATE_CLIENTS);
                     }
 
-                    return true;
+                    return io.fand.server.event.BlockEvents.finishStructureGrow(level, pos, fandSnapshot);
                 } else {
-                    level.setBlock(pos, state, Block.UPDATE_NONE);
+                    if (fandSnapshot != null) {
+                        io.fand.server.event.BlockEvents.restoreStructureGrow(level, fandSnapshot);
+                    } else {
+                        level.setBlock(pos, state, Block.UPDATE_NONE);
+                    }
                     return false;
                 }
             }
diff --git a/net/minecraft/world/level/portal/PortalForcer.java b/net/minecraft/world/level/portal/PortalForcer.java
index 1247b47f564c1b8703c1c9fa57c38e3f27a06f09..c4c5bdd140969f7cba1d6e3080c6e8e61dd22e7b 100644
--- a/net/minecraft/world/level/portal/PortalForcer.java
+++ b/net/minecraft/world/level/portal/PortalForcer.java
@@ -10,6 +10,7 @@ import net.minecraft.core.Vec3i;
 import net.minecraft.server.level.ServerLevel;
 import net.minecraft.util.BlockUtil;
 import net.minecraft.util.Mth;
+import net.minecraft.world.entity.Entity;
 import net.minecraft.world.entity.ai.village.poi.PoiManager;
 import net.minecraft.world.entity.ai.village.poi.PoiRecord;
 import net.minecraft.world.entity.ai.village.poi.PoiTypes;
@@ -53,6 +54,10 @@ public class PortalForcer {
     }
 
     public Optional<BlockUtil.FoundRectangle> createPortal(final BlockPos origin, final Direction.Axis portalAxis) {
+        return this.createPortal(origin, portalAxis, null);
+    }
+
+    public Optional<BlockUtil.FoundRectangle> createPortal(final BlockPos origin, final Direction.Axis portalAxis, final Entity entity) {
         Direction direction = Direction.get(Direction.AxisDirection.POSITIVE, portalAxis);
         double closestFullDistanceSqr = -1.0;
         BlockPos closestFullPosition = null;
@@ -153,6 +158,9 @@ public class PortalForcer {
                 io.fand.api.event.block.PortalCreateEvent.Cause.EXIT_PORTAL)) {
             return Optional.empty();
         }
+        if (entity != null && !io.fand.server.event.EntityEvents.fireCreatePortal(entity, this.level, fandPortalBlocks)) {
+            return Optional.empty();
+        }
 
         for (int width = -1; width < 3; width++) {
             for (int height = -1; height < 4; height++) {
