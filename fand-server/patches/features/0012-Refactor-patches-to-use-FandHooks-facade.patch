From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: MC20018 <winme2001@outlook.com>
Date: Sat, 6 Jun 2026 14:09:44 +0800
Subject: [PATCH] Refactor patches to use FandHooks facade


diff --git a/net/minecraft/server/level/ServerPlayer.java b/net/minecraft/server/level/ServerPlayer.java
index 6486ce6a8d7dd3b357ef7277f9301c3515667171..7d3c8d625db233d1fc8d730f4eb1a1c81c99cd8a 100644
--- a/net/minecraft/server/level/ServerPlayer.java
+++ b/net/minecraft/server/level/ServerPlayer.java
@@ -895,8 +895,8 @@ public class ServerPlayer extends Player {
         this.gameEvent(GameEvent.ENTITY_DIE);
         boolean showDeathMessage = this.level().getGameRules().get(GameRules.SHOW_DEATH_MESSAGES);
         Component deathMessage = showDeathMessage ? this.getCombatTracker().getDeathMessage() : null;
-        if (io.fand.server.Main.runtime().events().hasListeners(io.fand.api.event.player.PlayerDeathEvent.class)) {
-            var fandPlayer = io.fand.server.Main.runtime().playerRegistry().find(this.getUUID()).orElse(null);
+        if (io.fand.server.hooks.FandHooks.hasListeners(io.fand.api.event.player.PlayerDeathEvent.class)) {
+            var fandPlayer = io.fand.server.hooks.FandHooks.findPlayer(this.getUUID());
             if (fandPlayer != null) {
                 net.kyori.adventure.text.Component fandDeathMessage = null;
                 if (deathMessage != null) {
@@ -906,12 +906,9 @@ public class ServerPlayer extends Player {
                         LOGGER.warn("Skipping PlayerDeathEvent after fromVanilla failure", fandFailure);
                     }
                 }
-                var fandDeathEvent = new io.fand.api.event.player.PlayerDeathEvent(fandPlayer, fandDeathMessage);
-                try {
-                    io.fand.server.Main.runtime().events().fire(fandDeathEvent);
-                } catch (RuntimeException fandFailure) {
-                    LOGGER.warn("PlayerDeathEvent listener failed", fandFailure);
-                }
+                var fandDeathEvent = io.fand.server.hooks.FandHooks.fireOrLog(
+                        new io.fand.api.event.player.PlayerDeathEvent(fandPlayer, fandDeathMessage),
+                        "PlayerDeathEvent");
                 if (fandDeathEvent.deathMessage() == null) {
                     showDeathMessage = false;
                     deathMessage = null;
diff --git a/net/minecraft/server/level/ServerPlayerGameMode.java b/net/minecraft/server/level/ServerPlayerGameMode.java
index 4f38ac40b1e702a3e77be1fbc6f3055a95c0cc74..7c3aea0895aa1ebbe9487bcd5903fb0551a19446 100644
--- a/net/minecraft/server/level/ServerPlayerGameMode.java
+++ b/net/minecraft/server/level/ServerPlayerGameMode.java
@@ -272,19 +272,16 @@ public class ServerPlayerGameMode {
             } else if (this.player.blockActionRestricted(this.level, pos, this.gameModeForPlayer)) {
                 return false;
             } else {
-                if (io.fand.server.Main.runtime().events().hasListeners(io.fand.api.event.block.BlockBreakEvent.class)) {
-                    var fandPlayer = io.fand.server.Main.runtime().playerRegistry().find(this.player.getUUID()).orElse(null);
+                if (io.fand.server.hooks.FandHooks.hasListeners(io.fand.api.event.block.BlockBreakEvent.class)) {
+                    var fandPlayer = io.fand.server.hooks.FandHooks.findPlayer(this.player.getUUID());
                     if (fandPlayer != null) {
-                        var fandWorld = io.fand.server.Main.runtime().worldRegistry().map(r -> r.wrap(this.level)).orElse(null);
+                        var fandWorld = io.fand.server.hooks.FandHooks.wrapWorld(this.level);
                         if (fandWorld != null) {
                             var fandBlock = new io.fand.server.block.FandBlock(fandWorld, pos.getX(), pos.getY(), pos.getZ());
                             var fandBlockType = io.fand.server.block.FandBlockType.of(block);
-                            var fandBreakEvent = new io.fand.api.event.block.BlockBreakEvent(fandPlayer, fandBlock, fandBlockType);
-                            try {
-                                io.fand.server.Main.runtime().events().fire(fandBreakEvent);
-                            } catch (RuntimeException fandFailure) {
-                                LOGGER.warn("BlockBreakEvent listener failed", fandFailure);
-                            }
+                            var fandBreakEvent = io.fand.server.hooks.FandHooks.fireOrLog(
+                                    new io.fand.api.event.block.BlockBreakEvent(fandPlayer, fandBlock, fandBlockType),
+                                    "BlockBreakEvent");
                             if (fandBreakEvent.cancelled()) {
                                 this.level.sendBlockUpdated(pos, state, state, Block.UPDATE_ALL);
                                 return false;
diff --git a/net/minecraft/server/network/ServerGamePacketListenerImpl.java b/net/minecraft/server/network/ServerGamePacketListenerImpl.java
index 4d68a9bd0cf3abb62e3cfecc9e5469795e82a776..2ba6a8cc3f6caef06a0320d46071376cff51226a 100644
--- a/net/minecraft/server/network/ServerGamePacketListenerImpl.java
+++ b/net/minecraft/server/network/ServerGamePacketListenerImpl.java
@@ -1160,21 +1160,18 @@ public class ServerGamePacketListenerImpl
                                     || (!fail || !level.noCollision(this.player, oldAABB))
                                         && !this.isEntityCollidingWithAnythingNew(level, this.player, oldAABB, targetX, targetY, targetZ)) {
                                     boolean fandMoveCancelled = false;
-                                    if (io.fand.server.Main.runtime().events().hasListeners(io.fand.api.event.player.PlayerMoveEvent.class)
+                                    if (io.fand.server.hooks.FandHooks.hasListeners(io.fand.api.event.player.PlayerMoveEvent.class)
                                             && (startX != targetX || startY != targetY || startZ != targetZ
                                                     || this.player.getYRot() != targetYRot || this.player.getXRot() != targetXRot)) {
-                                        var fandPlayer = io.fand.server.Main.runtime().playerRegistry().find(this.player.getUUID()).orElse(null);
+                                        var fandPlayer = io.fand.server.hooks.FandHooks.findPlayer(this.player.getUUID());
                                         if (fandPlayer != null) {
-                                            var fandWorld = io.fand.server.Main.runtime().worldRegistry().map(r -> r.wrap(level)).orElse(null);
+                                            var fandWorld = io.fand.server.hooks.FandHooks.wrapWorld(level);
                                             if (fandWorld != null) {
                                                 var fandFrom = new io.fand.api.world.Location(fandWorld, startX, startY, startZ, this.player.getYRot(), this.player.getXRot());
                                                 var fandTo = new io.fand.api.world.Location(fandWorld, targetX, targetY, targetZ, targetYRot, targetXRot);
-                                                var fandMoveEvent = new io.fand.api.event.player.PlayerMoveEvent(fandPlayer, fandFrom, fandTo);
-                                                try {
-                                                    io.fand.server.Main.runtime().events().fire(fandMoveEvent);
-                                                } catch (RuntimeException fandFailure) {
-                                                    LOGGER.warn("PlayerMoveEvent listener failed", fandFailure);
-                                                }
+                                                var fandMoveEvent = io.fand.server.hooks.FandHooks.fireOrLog(
+                                                        new io.fand.api.event.player.PlayerMoveEvent(fandPlayer, fandFrom, fandTo),
+                                                        "PlayerMoveEvent");
                                                 fandMoveCancelled = fandMoveEvent.cancelled();
                                             }
                                         }
@@ -1393,25 +1390,22 @@ public class ServerGamePacketListenerImpl
                             if (this.server.isUnderSpawnProtection(level, pos, this.player)) {
                                 this.player.sendSpawnProtectionMessage(pos);
                             } else if (this.awaitingPositionFromClient == null && level.mayInteract(this.player, pos)) {
-                                if (io.fand.server.Main.runtime().events().hasListeners(io.fand.api.event.player.PlayerInteractEvent.class)) {
-                                    var fandPlayer = io.fand.server.Main.runtime().playerRegistry().find(this.player.getUUID()).orElse(null);
+                                if (io.fand.server.hooks.FandHooks.hasListeners(io.fand.api.event.player.PlayerInteractEvent.class)) {
+                                    var fandPlayer = io.fand.server.hooks.FandHooks.findPlayer(this.player.getUUID());
                                     if (fandPlayer != null) {
-                                        var fandWorld = io.fand.server.Main.runtime().worldRegistry().map(r -> r.wrap(level)).orElse(null);
+                                        var fandWorld = io.fand.server.hooks.FandHooks.wrapWorld(level);
                                         var fandBlock = fandWorld == null ? java.util.Optional.<io.fand.api.block.Block>empty()
                                                 : java.util.Optional.<io.fand.api.block.Block>of(new io.fand.server.block.FandBlock(fandWorld, pos.getX(), pos.getY(), pos.getZ()));
                                         var fandHand = hand == net.minecraft.world.InteractionHand.OFF_HAND
                                                 ? io.fand.api.event.player.PlayerInteractEvent.Hand.OFF_HAND
                                                 : io.fand.api.event.player.PlayerInteractEvent.Hand.MAIN_HAND;
-                                        var fandInteractEvent = new io.fand.api.event.player.PlayerInteractEvent(
-                                                fandPlayer,
-                                                io.fand.api.event.player.PlayerInteractEvent.Action.RIGHT_CLICK_BLOCK,
-                                                fandHand,
-                                                fandBlock);
-                                        try {
-                                            io.fand.server.Main.runtime().events().fire(fandInteractEvent);
-                                        } catch (RuntimeException fandFailure) {
-                                            LOGGER.warn("PlayerInteractEvent listener failed", fandFailure);
-                                        }
+                                        var fandInteractEvent = io.fand.server.hooks.FandHooks.fireOrLog(
+                                                new io.fand.api.event.player.PlayerInteractEvent(
+                                                        fandPlayer,
+                                                        io.fand.api.event.player.PlayerInteractEvent.Action.RIGHT_CLICK_BLOCK,
+                                                        fandHand,
+                                                        fandBlock),
+                                                "PlayerInteractEvent");
                                         if (fandInteractEvent.cancelled()) {
                                             this.send(new ClientboundBlockUpdatePacket(level, pos));
                                             this.send(new ClientboundBlockUpdatePacket(level, pos.relative(direction)));
@@ -1480,22 +1474,19 @@ public class ServerGamePacketListenerImpl
                     this.player.absSnapRotationTo(targetYRot, targetXRot);
                 }
 
-                if (io.fand.server.Main.runtime().events().hasListeners(io.fand.api.event.player.PlayerInteractEvent.class)) {
-                    var fandPlayer = io.fand.server.Main.runtime().playerRegistry().find(this.player.getUUID()).orElse(null);
+                if (io.fand.server.hooks.FandHooks.hasListeners(io.fand.api.event.player.PlayerInteractEvent.class)) {
+                    var fandPlayer = io.fand.server.hooks.FandHooks.findPlayer(this.player.getUUID());
                     if (fandPlayer != null) {
                         var fandHand = hand == net.minecraft.world.InteractionHand.OFF_HAND
                                 ? io.fand.api.event.player.PlayerInteractEvent.Hand.OFF_HAND
                                 : io.fand.api.event.player.PlayerInteractEvent.Hand.MAIN_HAND;
-                        var fandInteractEvent = new io.fand.api.event.player.PlayerInteractEvent(
-                                fandPlayer,
-                                io.fand.api.event.player.PlayerInteractEvent.Action.RIGHT_CLICK_AIR,
-                                fandHand,
-                                java.util.Optional.empty());
-                        try {
-                            io.fand.server.Main.runtime().events().fire(fandInteractEvent);
-                        } catch (RuntimeException fandFailure) {
-                            LOGGER.warn("PlayerInteractEvent listener failed", fandFailure);
-                        }
+                        var fandInteractEvent = io.fand.server.hooks.FandHooks.fireOrLog(
+                                new io.fand.api.event.player.PlayerInteractEvent(
+                                        fandPlayer,
+                                        io.fand.api.event.player.PlayerInteractEvent.Action.RIGHT_CLICK_AIR,
+                                        fandHand,
+                                        java.util.Optional.empty()),
+                                "PlayerInteractEvent");
                         if (fandInteractEvent.cancelled()) {
                             return;
                         }
@@ -1589,7 +1580,7 @@ public class ServerGamePacketListenerImpl
                 CompletableFuture<FilteredText> filteredFuture = this.filterTextPacket(signedMessage.signedContent());
                 Component decorated = this.server.getChatDecorator().decorate(this.player, signedMessage.decoratedContent());
                 this.chatMessageChain.append(filteredFuture, filtered -> {
-                    var fandPlayer = io.fand.server.Main.runtime().playerRegistry().find(this.player.getUUID()).orElse(null);
+                    var fandPlayer = io.fand.server.hooks.FandHooks.findPlayer(this.player.getUUID());
                     Component fandDecorated = decorated;
                     if (fandPlayer != null) {
                         net.kyori.adventure.text.Component fandMessage;
@@ -1600,12 +1591,9 @@ public class ServerGamePacketListenerImpl
                             fandMessage = null;
                         }
                         if (fandMessage != null) {
-                            var fandChatEvent = new io.fand.api.event.player.PlayerChatEvent(fandPlayer, packet.message(), fandMessage);
-                            try {
-                                io.fand.server.Main.runtime().events().fire(fandChatEvent);
-                            } catch (RuntimeException fandFailure) {
-                                LOGGER.warn("PlayerChatEvent listener failed", fandFailure);
-                            }
+                            var fandChatEvent = io.fand.server.hooks.FandHooks.fireOrLog(
+                                    new io.fand.api.event.player.PlayerChatEvent(fandPlayer, packet.message(), fandMessage),
+                                    "PlayerChatEvent");
                             if (fandChatEvent.cancelled()) {
                                 return;
                             }
diff --git a/net/minecraft/server/players/PlayerList.java b/net/minecraft/server/players/PlayerList.java
index 8cc4e0e82df5fd229fa2c310b921f7996d7da0b9..10f39f76e83482f07d4bf129f34f28f6c51df2a9 100644
--- a/net/minecraft/server/players/PlayerList.java
+++ b/net/minecraft/server/players/PlayerList.java
@@ -209,13 +209,10 @@ public abstract class PlayerList {
         net.kyori.adventure.text.Component fandJoinMessage = io.fand.server.command.AdventureBridge.fromVanilla(
                 fandJoinFallback, this.server.registryAccess()
         );
-        io.fand.server.entity.FandPlayer fandPlayer = io.fand.server.Main.runtime().playerRegistry().attach(player);
-        var fandJoinEvent = new io.fand.api.event.player.PlayerJoinEvent(fandPlayer, fandJoinMessage);
-        try {
-            io.fand.server.Main.runtime().events().fire(fandJoinEvent);
-        } catch (RuntimeException fandFailure) {
-            LOGGER.warn("PlayerJoinEvent listener failed", fandFailure);
-        }
+        io.fand.server.entity.FandPlayer fandPlayer = io.fand.server.hooks.FandHooks.players().attach(player);
+        var fandJoinEvent = io.fand.server.hooks.FandHooks.fireOrLog(
+                new io.fand.api.event.player.PlayerJoinEvent(fandPlayer, fandJoinMessage),
+                "PlayerJoinEvent");
         var fandFinalJoinMessage = fandJoinEvent.message();
         if (fandFinalJoinMessage != null) {
             this.broadcastSystemMessage(io.fand.server.command.AdventureBridge.toVanillaOrFallback(fandFinalJoinMessage, fandJoinFallback, this.server.registryAccess()), false);
@@ -324,14 +321,11 @@ public abstract class PlayerList {
 
     public void remove(final ServerPlayer player) {
         ServerLevel level = player.level();
-        var fandPlayerForQuit = io.fand.server.Main.runtime().playerRegistry().detach(player.getUUID()).orElse(null);
+        var fandPlayerForQuit = io.fand.server.hooks.FandHooks.players().detach(player.getUUID()).orElse(null);
         if (fandPlayerForQuit != null) {
-            var fandQuitEvent = new io.fand.api.event.player.PlayerQuitEvent(fandPlayerForQuit, null);
-            try {
-                io.fand.server.Main.runtime().events().fire(fandQuitEvent);
-            } catch (RuntimeException fandFailure) {
-                LOGGER.warn("PlayerQuitEvent listener failed", fandFailure);
-            }
+            var fandQuitEvent = io.fand.server.hooks.FandHooks.fireOrLog(
+                    new io.fand.api.event.player.PlayerQuitEvent(fandPlayerForQuit, null),
+                    "PlayerQuitEvent");
             var fandFinalQuitMessage = fandQuitEvent.message();
             if (fandFinalQuitMessage != null) {
                 try {
@@ -459,7 +453,7 @@ public abstract class PlayerList {
         level.addRespawnedPlayer(player);
         this.players.add(player);
         this.playersByUUID.put(player.getUUID(), player);
-        io.fand.server.Main.runtime().playerRegistry().onRespawn(player);
+        io.fand.server.hooks.FandHooks.players().onRespawn(player);
         player.initInventoryMenu();
         player.setHealth(player.getHealth());
         ServerPlayer.RespawnConfig respawnConfig = player.getRespawnConfig();
diff --git a/net/minecraft/world/entity/LivingEntity.java b/net/minecraft/world/entity/LivingEntity.java
index 2e1374a72f8582c1668ed9b2c629704d2a9afd4c..9940945fc4af7d4c4326a6862c23052bf7b93edf 100644
--- a/net/minecraft/world/entity/LivingEntity.java
+++ b/net/minecraft/world/entity/LivingEntity.java
@@ -1157,24 +1157,21 @@ public abstract class LivingEntity extends Entity implements Attackable, Waypoin
         } else if (source.is(DamageTypeTags.IS_FIRE) && this.hasEffect(MobEffects.FIRE_RESISTANCE)) {
             return false;
         } else {
-            if (io.fand.server.Main.runtime().events().hasListeners(io.fand.api.event.entity.EntityDamageEvent.class)) {
+            if (io.fand.server.hooks.FandHooks.hasListeners(io.fand.api.event.entity.EntityDamageEvent.class)) {
                 io.fand.api.entity.LivingEntity fandVictim = null;
                 if (this instanceof net.minecraft.server.level.ServerPlayer fandServerPlayer) {
-                    fandVictim = io.fand.server.Main.runtime().playerRegistry().find(fandServerPlayer.getUUID()).orElse(null);
+                    fandVictim = io.fand.server.hooks.FandHooks.findPlayer(fandServerPlayer.getUUID());
                 } else {
-                    var fandWorldRegistry = io.fand.server.Main.runtime().worldRegistry().orElse(null);
-                    if (fandWorldRegistry != null) {
-                        fandVictim = new io.fand.server.entity.FandLivingEntity(this, fandWorldRegistry);
+                    var fandEntityRegistry = io.fand.server.hooks.FandHooks.entities().orElse(null);
+                    if (fandEntityRegistry != null) {
+                        fandVictim = fandEntityRegistry.wrap(this);
                     }
                 }
                 if (fandVictim != null) {
                     var fandDamageType = source.typeHolder().unwrapKey().map(k -> k.identifier().toString()).orElse("minecraft:generic");
-                    var fandDamageEvent = new io.fand.api.event.entity.EntityDamageEvent(fandVictim, fandDamageType, damage);
-                    try {
-                        io.fand.server.Main.runtime().events().fire(fandDamageEvent);
-                    } catch (RuntimeException fandFailure) {
-                        org.slf4j.LoggerFactory.getLogger(LivingEntity.class).warn("EntityDamageEvent listener failed", fandFailure);
-                    }
+                    var fandDamageEvent = io.fand.server.hooks.FandHooks.fireOrLog(
+                            new io.fand.api.event.entity.EntityDamageEvent(fandVictim, fandDamageType, damage),
+                            "EntityDamageEvent");
                     if (fandDamageEvent.cancelled()) {
                         return false;
                     }
diff --git a/net/minecraft/world/inventory/AbstractContainerMenu.java b/net/minecraft/world/inventory/AbstractContainerMenu.java
index 39e98038d74c1544b4a6133853b30a0876dec83f..21b5231361c2c3860e1ee6aeeff1947b54522b81 100644
--- a/net/minecraft/world/inventory/AbstractContainerMenu.java
+++ b/net/minecraft/world/inventory/AbstractContainerMenu.java
@@ -374,6 +374,19 @@ public abstract class AbstractContainerMenu {
                         return;
                     }
 
+                    if (player instanceof net.minecraft.server.level.ServerPlayer serverPlayer) {
+                        java.util.Set<Integer> indices = new java.util.LinkedHashSet<>();
+                        for (Slot slot : this.quickcraftSlots) {
+                            indices.add(slot.index);
+                        }
+                        if (!io.fand.server.inventory.InventoryEvents.fireDrag(
+                                serverPlayer, this, this.quickcraftType, indices)) {
+                            this.resetQuickCraft();
+                            this.broadcastFullState();
+                            return;
+                        }
+                    }
+
                     int remaining = this.getCarried().getCount();
 
                     for (Slot slot : this.quickcraftSlots) {
diff --git a/net/minecraft/world/item/BlockItem.java b/net/minecraft/world/item/BlockItem.java
index 1b4dcc56709e4708cc2f4aefb2092fb4cddb719e..d671086d0b09c217e19cd5853b34df40d327a29e 100644
--- a/net/minecraft/world/item/BlockItem.java
+++ b/net/minecraft/world/item/BlockItem.java
@@ -68,21 +68,18 @@ public class BlockItem extends Item {
                     }
                     if (fandLevel instanceof net.minecraft.server.level.ServerLevel fandServerLevel
                             && fandContextPlayer instanceof ServerPlayer fandServerPlayer
-                            && io.fand.server.Main.runtime().events().hasListeners(io.fand.api.event.block.BlockPlaceEvent.class)) {
-                        var fandPlayer = io.fand.server.Main.runtime().playerRegistry().find(fandServerPlayer.getUUID()).orElse(null);
+                            && io.fand.server.hooks.FandHooks.hasListeners(io.fand.api.event.block.BlockPlaceEvent.class)) {
+                        var fandPlayer = io.fand.server.hooks.FandHooks.findPlayer(fandServerPlayer.getUUID());
                         if (fandPlayer != null) {
-                            var fandWorld = io.fand.server.Main.runtime().worldRegistry().map(r -> r.wrap(fandServerLevel)).orElse(null);
+                            var fandWorld = io.fand.server.hooks.FandHooks.wrapWorld(fandServerLevel);
                             if (fandWorld != null) {
                                 BlockState fandPlacedState = fandLevel.getBlockState(fandPos);
                                 var fandBlock = new io.fand.server.block.FandBlock(fandWorld, fandPos.getX(), fandPos.getY(), fandPos.getZ());
                                 var fandPlacedType = io.fand.server.block.FandBlockType.of(fandPlacedState.getBlock());
                                 var fandReplacedType = io.fand.server.block.FandBlockType.of(fandReplacedState.getBlock());
-                                var fandPlaceEvent = new io.fand.api.event.block.BlockPlaceEvent(fandPlayer, fandBlock, fandPlacedType, fandReplacedType);
-                                try {
-                                    io.fand.server.Main.runtime().events().fire(fandPlaceEvent);
-                                } catch (RuntimeException fandFailure) {
-                                    org.slf4j.LoggerFactory.getLogger(BlockItem.class).warn("BlockPlaceEvent listener failed", fandFailure);
-                                }
+                                var fandPlaceEvent = io.fand.server.hooks.FandHooks.fireOrLog(
+                                        new io.fand.api.event.block.BlockPlaceEvent(fandPlayer, fandBlock, fandPlacedType, fandReplacedType),
+                                        "BlockPlaceEvent");
                                 if (fandPlaceEvent.cancelled()) {
                                     fandLevel.setBlock(fandPos, fandReplacedState, Block.UPDATE_ALL);
                                     fandServerPlayer.connection.send(new net.minecraft.network.protocol.game.ClientboundBlockUpdatePacket(fandLevel, fandPos));
